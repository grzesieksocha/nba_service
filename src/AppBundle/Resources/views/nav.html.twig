<nav class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <a class="navbar-brand" href="{{ path('homepage') }}"><img src="{{ asset('images/nbaplayoffs.svg') }}" height="30px" style="margin-top: -5px;"></a>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
            <form class="navbar-form navbar-right">
            {% if app.user %}
                Logged in!!!
                <a  class="btn btn-success blue" href="{{ path('fos_user_security_logout') }}">Log out</a>
            {% else %}
                    <div class="form-group">
                        <input type="text" placeholder="Email" class="form-control">
                    </div>
                    <div class="form-group">
                        <input type="password" placeholder="Password" class="form-control">
                    </div>
                    <button type="submit" class="btn btn-success blue">Sign in</button>
                    <a  class="btn btn-success blue" href="{{ path('fos_user_registration_register') }}">Register</a>
            {% endif %}
            </form>
        </div>
    </div>
</nav>